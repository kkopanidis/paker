{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core:default (events/listen, path helpers, read-only window/webview metadata) + opener:default (https/http/mailto/tel URLs, reveal-in-finder). No fs, dialog, http, or shell plugins.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
